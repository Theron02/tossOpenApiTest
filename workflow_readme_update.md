# WORKFLOW: 작업 완료 시 README 갱신 규칙

이 문서는 **상시 규칙**이다. Claude Code는 어떤 작업(TASK)을 완료할 때마다
이 절차에 따라 루트 `README.md`를 갱신한다. 일회성 지시가 아니라 매 작업의 마무리 단계다.

---

## 언제 갱신하는가

다음 중 하나라도 해당하면 README를 갱신한다.
- TASK 문서(`back/docs/TASK_*.md`) 하나를 완료했을 때
- "개발 현황" 체크리스트의 항목 상태가 바뀌었을 때
- 새 기능·모듈·외부 연동이 추가되어 아키텍처/구조 설명이 달라졌을 때
- 스택·실행 방법·환경변수가 추가·변경됐을 때

---

## 무엇을 갱신하는가

`README.md`에서 아래 섹션을 작업 내용에 맞게 수정한다.

1. **개발 현황 체크리스트**
    - 완료한 항목을 `- [ ]` → `- [x]` 로 변경.
    - 작업 중 새로 생긴 후속 항목이 있으면 적절한 위치에 추가.

2. **프로젝트 구조**
    - 새 디렉토리·주요 파일이 생겼으면 트리에 반영.

3. **기술 스택 / 환경변수**
    - 새 의존성·외부 서비스·필수 환경변수가 추가됐으면 표·목록 갱신.

4. **실행 방법** (해당 섹션이 있으면)
    - 빌드·기동·테스트 커맨드가 바뀌었으면 갱신.

5. **아키텍처**
    - 컴포넌트가 추가/변경됐으면 다이어그램 텍스트 수정.

---

## 어떻게 갱신하는가 (절차)

작업 완료 직후, 코드 커밋 전에:

1. 방금 완료한 TASK 문서를 다시 읽고, 그 산출물이 README의 어느 섹션에 영향을 주는지 식별.
2. 해당 섹션만 **국소적으로** 수정한다. 관련 없는 부분은 건드리지 않는다.
3. 체크리스트 상태를 정확히 반영한다 (실제로 완료·검증된 것만 `[x]`).
4. 갱신 내용을 커밋 메시지에 함께 남긴다 (예: `docs: update README after TASK_01 entities`).

---

## 지킬 것

- **사실만 반영한다.** 아직 구현·검증되지 않은 항목을 완료로 표시하지 않는다.
- 톤·구조를 유지한다. 기존 README의 형식(섹션 순서, 표기)을 따른다.
- 비밀값·실제 연결정보·키를 README에 절대 쓰지 않는다.
- "최종 목표는 실거래, 모의투자 우선" 기조를 유지한다. 이 방향성을 바꾸지 않는다.
- README는 사람이 처음 레포를 볼 때의 지도다. 장황하게 늘리지 말고 최신·정확하게 유지한다.

---

## 예시

TASK_01(엔티티) 완료 시:
```diff
- - [ ] JPA 엔티티 + enum 구현
+ - [x] JPA 엔티티 + enum 구현
```
+ 프로젝트 구조 트리에 `back/.../entity/`, `domain/type/` 추가.

TASK_02-A(DB 연결 테스트) 완료 시:
```diff
  ## 기술 스택
+ 환경변수: SUPABASE_DB_HOST, SUPABASE_DB_PORT, SUPABASE_DB_NAME,
+           SUPABASE_DB_USER, SUPABASE_DB_PASSWORD
```
+ (실행 방법 섹션이 있으면) DB 연결 테스트 커맨드 추가.

---

## CLAUDE.md 연동

이 규칙이 항상 적용되도록, 루트 `CLAUDE.md`의 작업 지침에 다음 한 줄을 둔다:

> 모든 TASK를 완료하면 `back/docs/WORKFLOW_readme_update.md` 규칙에 따라
> 루트 `README.md`를 갱신한다.